class State:
    def __init__(self, current_node, visited, current_cost, total_value):
        self.current_node = current_node
        self.visited = visited
        self.current_cost = current_cost
        self.total_value = total_value
